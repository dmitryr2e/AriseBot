# Верхние границы обязательны: без них любой мажорный релиз зависимости
# попадает в следующий docker build без единого коммита у нас.
aiogram>=3.13,<4
aiosqlite>=0.22.1,<1
APScheduler>=3.10,<4
Pillow>=10.0,<12
httpx>=0.27,<1
qrcode[pil]>=7.4,<9
